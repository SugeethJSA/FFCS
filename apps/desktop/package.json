{
  "name": "desktop",
  "version": "1.0.0",
  "description": "FFCS Track Desktop — Tauri wrapper for FFCS Attendance (Meet + QR)",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
